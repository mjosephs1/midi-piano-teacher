# Midi Piano Teacher

## Setup
To install dependencies, open a terminal and enter the command: 
- `pip install -r requirements.txt`

## Usage
To run the app, open a terminal and enter `python3 main.py`